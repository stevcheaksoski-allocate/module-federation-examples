import React from 'react'

const AllocateEntry = (props: any) => {
    return <>
        <h1> This is Allocate entry component ready to be imported.</h1>
    </>
}

export default AllocateEntry;